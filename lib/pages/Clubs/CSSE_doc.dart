import 'package:flutter/material.dart';

class CSSEDoc extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
     
      body: Center(
        child: Text('This is the new page!'),
      ),
    );
  }
}